# Startup Agent Workspace

This workspace contains a simple scaffold for a frontend and backend:

- frontend/: Static web app (public + src + components)
- backend/: Simple API server (src + routes + controllers + models)

Run instructions and dependencies are intentionally minimal — customize as needed.
